{
    "name": "boxoab/boxoab",
    "type": "project",
    "description": "Plataforma de estudos Box OAB",
    "require": {
        "php": "^8.2",
        "laravel/framework": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "scripts": {
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ]
    }
}
